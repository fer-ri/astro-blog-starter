---
import Item from "./item.component.astro";

interface Props {
  url?: string;
}
const { url } = Astro.props;
---

<Item
  as="a"
  href={url}
  class:list={[{ "pointer-events-none cursor-default": !url }]}
  aria-label={url && `Goto to previous page`}
>
  <slot>
    <svg
      xmlns="http://www.w3.org/2000/svg"
      fill="none"
      viewBox="0 0 24 24"
      stroke-width="1.5"
      stroke="currentColor"
      class="w-6 h-6"
    >
      <path
        stroke-linecap="round"
        stroke-linejoin="round"
        d="M6.75 15.75L3 12m0 0l3.75-3.75M3 12h18"></path>
    </svg>
  </slot>
</Item>
